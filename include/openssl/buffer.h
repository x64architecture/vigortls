/*
 * Copyright 1995-2016 The OpenSSL Project Authors. All Rights Reserved.
 *
 * Licensed under the OpenSSL license (the "License").  You may not use
 * this file except in compliance with the License.  You can obtain a copy
 * in the file LICENSE in the source distribution or at
 * https://www.openssl.org/source/license.html
 */

#ifndef HEADER_BUFFER_H
#define HEADER_BUFFER_H

#include <stddef.h>
#include <stdint.h>

#if !defined(_WIN32)
#include <sys/types.h>
#endif

#include <openssl/base.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Already declared in ossl_typ.h */
/* typedef struct buf_mem_st BUF_MEM; */

struct buf_mem_st {
    size_t length; /* current number of bytes */
    char *data;
    size_t max; /* size of buffer */
};

VIGORTLS_EXPORT BUF_MEM *BUF_MEM_new(void);
VIGORTLS_EXPORT void BUF_MEM_free(BUF_MEM *a);
VIGORTLS_EXPORT int BUF_MEM_grow(BUF_MEM *str, size_t len);
VIGORTLS_EXPORT int BUF_MEM_grow_clean(BUF_MEM *str, size_t len);
#ifndef VIGORTLS_IMPLEMENTATION
VIGORTLS_EXPORT size_t BUF_strnlen(const char *str, size_t maxlen);
VIGORTLS_EXPORT char *BUF_strdup(const char *str);
VIGORTLS_EXPORT char *BUF_strndup(const char *str, size_t siz);
VIGORTLS_EXPORT void *BUF_memdup(const void *data, size_t siz);
#endif
VIGORTLS_EXPORT void BUF_reverse(uint8_t *out, const uint8_t *in, size_t siz);

/* safe string functions */
VIGORTLS_EXPORT size_t BUF_strlcpy(char *dst, const char *src, size_t siz);
VIGORTLS_EXPORT size_t BUF_strlcat(char *dst, const char *src, size_t siz);

/* BEGIN ERROR CODES */
/*
 * The following lines are auto generated by the script mkerr.pl. Any changes
 * made after this point may be overwritten when the script is next run.
 */
VIGORTLS_EXPORT void ERR_load_BUF_strings(void);

/* Error codes for the BUF functions. */

/* Function codes. */
# define BUF_F_BUF_MEMDUP                                 103
# define BUF_F_BUF_MEM_GROW                               100
# define BUF_F_BUF_MEM_GROW_CLEAN                         105
# define BUF_F_BUF_MEM_NEW                                101
# define BUF_F_BUF_STRDUP                                 102
# define BUF_F_BUF_STRNDUP                                104

/* Reason codes. */

#ifdef  __cplusplus
}
#endif
#endif
